server:
  port: 8080
  host: 0.0.0.0

database:
  driver: postgres
  dsn: ${DATABASE_URL}

jwt:
  secret: ${JWT_SECRET}
  expiry: 15m
  refresh_expiry: 168h  # 7 days

oauth:
  auth_code_expiry: 5m

log:
  level: info
  format: json
  file: /var/log/sso/sso.log
